import { PlaceholderPage } from "@/components/placeholder-page";

export default function ProvidersPage() {
  return (
    <PlaceholderPage
      title="AI Providers"
      description="Konfigurasi provider LLM (OpenAI, Anthropic, Gemini, OpenAI-compatible, local) dan kredensialnya. Provider abstraction sudah ready di worker."
      planned="Fase 3 (F3-02) - Provider abstraction ready, Providers UI TBD"
    />
  );
}
