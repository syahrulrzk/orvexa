import { PlaceholderPage } from "@/components/placeholder-page";

export default function SkillsPage() {
  return (
    <PlaceholderPage
      title="Skills"
      description="Katalog skill reusable yang bisa dipakai banyak agent (Linux, VLAN, Wazuh, Prometheus, dll)."
      planned="Fase 4"
    />
  );
}
